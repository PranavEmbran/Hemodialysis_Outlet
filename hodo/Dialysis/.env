# Vite Environment Variables
# All environment variables must be prefixed with VITE_ to be accessible in the frontend

# Data Mode Configuration
# Set this to control which data source the frontend uses:
# - 'mock': Uses localStorage-based mock data (no backend required)
# - 'real': Uses the Express + lowdb backend (default)
# - 'prod': Will be used for MSSQL backend integration (future)

VITE_DATA_MODE=real
# VITE_DATA_MODE=mock

# API Configuration
# Override the default API URL if needed
# If not set, the app will automatically determine the URL based on hostname
VITE_API_URL=http://192.168.50.50:5000/api

# Development Configuration
# VITE_DEV_MODE=true